import 'package:flutter/material.dart';

class UserHistoryTile extends StatelessWidget {
  const UserHistoryTile({super.key});

  @override
  Widget build(BuildContext context) {
    return ListTile(title: Text('User history'));
  }
}
