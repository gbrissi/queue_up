import 'package:flutter/material.dart';

class UserDlg extends StatelessWidget {
  const UserDlg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}